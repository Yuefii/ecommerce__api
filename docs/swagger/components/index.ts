import {
    image,
    review,
    product,
    productList,
} from "./schemas/products"

export const detailSchemas = {
    ProductList: productList,
    Product: product,
    Image: image,
    Review: review
}